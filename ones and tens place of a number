import random

def print_digits():
	num=random.randint(0,100)
	print ("the number generates is %d" %num)
	ones=num%10
	tens=num//10
	print ("The tens digit is %d, and the ones digit is %d." %(tens,ones))
	
print_digits()
